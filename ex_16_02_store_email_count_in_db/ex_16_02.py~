#exercice qui vise a recuperer les emails sender d'un fichier .txt en ligne et de remplir une database (sqlite) avec leur compte, par email (comme un dictionnaire)

import sqlite3

conn = sqlite3.connect('email_db.sqlite')

cur = conn.cursor()

cur.execute('''DROP
TABLE IF EXISTS Counts''')

